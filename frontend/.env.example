# API base for the frontend to reach the backend
# Example local: http://localhost:3000/api
VITE_API_URL=https://api.example.com/api

# Separate pay service URL if needed in UI
VITE_PAY_API_URL=https://pay.example.com

# Feature flags
VITE_SPONSOR_DYNAMIC_ALLOCATIONS=true

# Used in some flows/links; not exposed unless prefixed with VITE_,
# but included here for completeness as the repo currently references it.
QR_DESTINATION_URL=https://example.com/pay?paymentId={paymentId}